apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Container-to-container over the compose network, not localhost.
    url: http://prometheus:9090
    isDefault: true
